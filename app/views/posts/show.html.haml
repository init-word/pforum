#post_content 
 %h1= @post.title
 %p= @post.content
 #comments
  %h2= @post.comments.count
  = render @post.comments
  %h3 Comentar na thread
  = render "comments/form"

 = link_to "Editar", edit_post_path(@post)
 = link_to "Excluir", post_path(@post), method: :delete, data: { confirm: "Tem certeza que deseja excluir?" }
 = link_to "Home", root_path